/*var random = require("./random");


module.exports = class Eraser extends LiveForm {
    constructor(_x,_y,_LastCellId)
    {
        this.x = _x;
        this.y = _y;
        this.thisСellId;
        this.isActive = false;
    }
    moveRight()
    {
        if(!this.isActive)
        {
            matrix[y][x] = thisCellId;
            this.x++;
            thisCellId = matrix[y][x];
            matrix[y][x] = 6;
        }
        else
        {
            matrix[y][x] = 0;
            if(matrix[y][x] = 1)
            {

            }
            else if()
            {

            }
            else if()
            {
                
            }
            else if()
            {
                
            }
            this.x++;
            thisCellId = matrix[y][x];
            matrix[y][x] = 6;
        }
    }
    moveLeft()
    {
        if(!this.isActive)
        {
            matrix[y][x] = thisCellId;
            this.x--;
            thisCellId = matrix[y][x];
            matrix[y][x] = 6;
        }
        else
        {
            
        }
    }
    moveDown()
    {
        if(!this.isActive)
        {
            matrix[y][x] = thisCellId;
            this.y++;
            thisCellId = matrix[y][x];
            matrix[y][x] = 6;
        }
        else
        {
            
        }
    }
    moveUp()
    {
        if(!this.isActive)
        {
            matrix[y][x] = thisCellId;
            this.y--;
            thisCellId = matrix[y][x];
            matrix[y][x] = 6;
        }
        else
        {
            
        }
    }
    Active()
    {
        this.isActive = !this.isActive;
        this.thisCellId = matrix[y][x];
    }
    ChangeThisCellId()
    {
        this.thisCellId = matrix[][]
    }
}*/